# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Training runs: intermediate checkpoints, tensorboard logs, per-run
# metadata dumps. Final policies worth keeping are force-added explicitly
# (git add -f) rather than tracked wholesale.
checkpoints/
